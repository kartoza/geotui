app-id: com.kartoza.GeoTUI
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
command: geotui

finish-args:
  - --share=network
  - --filesystem=home

modules:
  - name: python3-geotui
    buildsystem: simple
    build-commands:
      - pip3 install --no-index --find-links=wheels --prefix=/app geotui
    sources:
      - type: dir
        path: ..
